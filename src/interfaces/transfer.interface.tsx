export interface TransferInterface {
  id: number;
  block: string;
  tx: string;
  from_address: string;
  to_address: string;
  amount: number;
  block_number: number;
  miner: string;
  created_date: string;
}
